@model List<DaemonCharacter.Domain.ItemAttributeModel>

@{
    Layout = null;
}


@foreach (var item in Model) {
    Html.RenderAction("Create", new { id = item.id
        , idAttribute = item.attribute.id
        , value = item.value });
}
